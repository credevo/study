# Codex 소개 페이지

OpenAI Codex를 간단히 소개하는 단일 정적 웹페이지입니다.

## 실행

별도 설치나 빌드 없이 `index.html`을 웹 브라우저로 엽니다.

```powershell
start index.html
```

## 구조와 환경

- `index.html`: 문서 구조와 인라인 CSS를 모두 포함한 페이지
- 최신 웹 브라우저만 필요하며 JavaScript와 외부 의존성은 사용하지 않습니다.
